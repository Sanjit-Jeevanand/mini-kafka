version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
  settings:
    errcheck:
      exclude-functions:
        - os.RemoveAll
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - govet
